"""Bot initialization. Run with: python -m app.bot.bot (polling for dev). Webhook mode uses FastAPI routes."""
# TODO: build Application with python-telegram-bot v20+, register handlers that call message_handler + dispatch actions.
# For webhook: handlers are invoked from api/routes/webhooks.telegram_webhook after parsing Update.
def main() -> None:
    pass


if __name__ == "__main__":
    main()
